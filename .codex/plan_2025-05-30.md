# Plan for 2025-05-30

## Completed on 2025-05-30
- Disabled Cython and C++ extension support by removing `CythonSupport` and the `src/c++` build in CMake.
- Committed these changes to the repository.

## Next Steps
1. Add comprehensive unit and integration tests for key pure-Python modules (e.g., Haplo.variant_processor, Tools.fastasize, Tools.vcfextract).
2. Replace external subprocess/shell dependencies (grep, samtools) with pure-Python implementations where feasible.
3. Update CI pipelines (Jenkinsfile and nox sessions) to use pip-based installation and pytest for the Python-only build.
4. Profile performance of core Haplo benchmarking functions and write microbenchmarks.
5. Plan migration path for remaining C++/Cython components beyond sequence_utils.
