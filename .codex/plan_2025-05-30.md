# Plan for 2025-05-30

## Completed on 2025-05-30
- Disabled Cython and C++ extension support by removing `CythonSupport` and the `src/c++` build in CMake.
- Added unit tests for key pure-Python modules (VariantProcessor, Tools.fastasize, Tools.vcfextract).
- Replaced several external subprocess/shell dependencies in FASTA utilities and VCF extraction with pure-Python implementations or proper error handling.
 Committed these changes to the repository.
 - Updated CI pipelines (Jenkinsfile and nox sessions) to use pip-based installation and pytest.

## Next Steps
1. Improve test coverage:
   - [ ] Add unit tests for all public APIs in Haplo and Tools modules.
   - [ ] Introduce end-to-end integration tests using representative VCF/FASTA datasets.
   - [ ] Integrate coverage measurement and enforce a minimum threshold (e.g., 90%).
2. Enforce type safety:
   - [x] Add type annotations to `Haplo/variant_processor.py` and test coverage for it.
   - [ ] Continue annotating other core modules (e.g., `sequence_utils.py`, core CLI logic).
   - [x] Enable mypy check in CI for `variant_processor.py` via `nox -s type_check`.
   - [ ] Incrementally fix additional type errors in future sessions.
3. Optimize performance:
   - [ ] Develop microbenchmarks with pytest-benchmark or nox sessions.
   - [ ] Profile hotspots in benchmarking and I/O utilities.
   - [ ] Address critical bottlenecks with algorithmic improvements or optional C-accelerated paths.
4. Enhance documentation and packaging:
   - [ ] Refactor CLI tools (`hap.py`, `qfy`, `pre`) to separate interface and core logic.
   - [ ] Document public APIs with docstrings and build Sphinx documentation.
   - [ ] Migrate project metadata fully to `pyproject.toml` using PEP 621.
5. CI/CD and developer experience:
   - [ ] Add GitHub Actions workflows for tests, linting, formatting, benchmarks, and coverage reporting.
   - [ ] Automate packaging checks and release processes.
6. Migration of native extensions:
   - [ ] Draft a detailed migration plan for remaining C++/Cython modules (e.g., sequence_utils).
   - [ ] Ensure documentation reflects deprecation of C++ and Cython modules.
   - [ ] Prioritize modules by usage impact and complexity for staged refactoring.
