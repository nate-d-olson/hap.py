# Plan for 2025-05-30

## Completed on 2025-05-30
- Disabled Cython and C++ extension support by removing `CythonSupport` and the `src/c++` build in CMake.
- Committed these changes to the repository.

## Next Steps
1. Advance to pure-Python PEP 517-only packaging: update `setup.py`, `setup.cfg`, and `pyproject.toml`.
2. Remove or archive CMake-based build files and related scripts (`configure.sh`, CMakeLists entries).
3. Add unit and integration tests for all pure-Python modules to ensure full coverage.
4. Refine CLI commands and verify console entry points (`hap.py`, `qfy`, `pre`).
5. Update project documentation (`README.md`, `AGENTS.md`) to reflect Python-only packaging workflow.
