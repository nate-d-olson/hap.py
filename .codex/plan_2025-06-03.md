# Development Plan for 2025-06-03

This plan captures the objectives, completed work, and next actions for the
third day of the on-going modernisation effort.

## High-level Objectives

1. Expose advanced RTG **vcfeval** options and outputs through the `hap.py` CLI
   while maintaining backward-compatibility with legacy invocations.
2. Increase observability and reproducibility by automatically generating:
   * a **tabix** index (`.tbi`) for the annotated VCF;
   * a **JSON** metrics file with all vcfeval statistics;
   * optional **ROC/PR** curve TSVs when `--roc` is requested.
3. Strengthen code health:
   * Raise the enforced coverage threshold from 85 % → **90 %** and include
     integration tests for the new CLI flags.
   * Expand type annotations in `Haplo.compare` and `happy.hap`.
4. Retire the old Jenkins pipeline and finish migrating CI to **GitHub
   Actions** (unit, integration, lint, type-check, coverage, packaging matrix).

## Progress Since 2025-06-02

✔ README and AGENTS documentation updated to reference the new plan file.
✔ Fixed minor TOC duplication in README.
✔ Skeleton for today’s plan created (this file).

## Detailed Task List

### 🏗  CLI Enhancements

| ID | Task | Owner | Status |
|----|------|-------|--------|
| C-1 | Add `--roc/--no-roc` flag mapping to vcfeval | @dev-1 | ✔ |
| C-2 | Add `--threads` integer option | @dev-1 | ✔ |
| C-3 | Add `--Xloose-match-distance <int>` passthrough | @dev-1 | ✔ |
| C-4 | Ensure `--write-json` writes `<prefix>.json` | @dev-1 | ✔ |
| C-5 | Auto-index output VCF with **tabix** | @dev-2 | ☐ |

### ✅  Test Coverage & QA

| ID | Task | Owner | Status |
|----|------|-------|--------|
| T-1 | Unit tests for new CLI options default/error cases | @qa | ✔ |
| T-2 | Integration test: `hap.py --roc` produces TSV | @qa | ✔ |
| T-3 | Coverage gate ≥ 90 % in `nox -s tests` | CI | ✔ |

### 📈  Type Safety & Linting

* Migrate `happy/hap.py` and `Haplo/vcfeval.py` to `from __future__ import
  annotations` and add full type hints. (Owner: @dev-2)
* Enable `mypy` strict settings for these modules only, expand gradually.

### ⚙️  CI Migration

1. Remove `Jenkinsfile` from required checks.
2. Add GitHub Actions workflows:
   * `python-tests.yml` – run unit & integration tests with coverage
   * `lint.yml` – ruff, black, pyupgrade
   * `type-check.yml` – mypy (incremental)

## Blockers / Risks

* **vcfeval** availability – ensure the test container or CI image includes
  `rtg` binary; otherwise mock binary for unit tests.
* Large example data sets slow down CI.  Mitigation: use down-sampled VCFs and
  `--chrom chr21` like the current integration suite.

## After 2025-06-03

* Performance profiling session focusing on I/O hotspots.
* Draft roadmap for deprecating the remaining Cython shims.

---

_Generated on 2025-06-03 by the Codex CLI agent._
