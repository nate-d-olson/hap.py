2025-06-04 Plan – α-release Preparation
======================================

This plan breaks down the remaining work toward the first α-quality release (v0.1.0a1).

1. End-to-End Benchmarking on HG002
   - [x] Configure and validate integration test for HG002 dataset
   - [x] Ensure reference FASTA, truth VCF, and BED regions are available
   - [x] Execute `hap.py` full-genome pipeline with default and custom settings
   - [ ] Verify JSON, ROC TSV, and summary reports for completeness and correctness
   - [ ] Investigate and resolve any pipeline failures or discrepancies

2. Auto-Index Creation for All Outputs (Task C-5)
   - [x] Audit all VCF/ROC/JSON writers for missing index hooks
   - [x] Integrate `_ensure_vcf_index()` immediately after writing each VCF output
   - [x] Add unit tests to confirm `.tbi` files are generated alongside VCFs
   - [x] Manually run sample pipeline to assert index presence next to each output

3. Performance Optimization for Large Genomes
   - [ ] Profile end-to-end runtime on HG002 to identify bottlenecks
   - [ ] Optimize I/O patterns, e.g. streaming vs. full data loads
   - [ ] Introduce multithreading or batching where safe and beneficial
   - [ ] Benchmark pre- and post-optimization and document gains

4. Packaging and Versioning
   - [ ] Bump package version to `0.1.0a1` in `setup.cfg` and/or `pyproject.toml`
   - [ ] Update `RELEASES.md` with alpha release notes and breaking-change warnings
   - [ ] Validate `pip install .` produces `hap.py`, `qfy`, and `pre` console scripts
   - [ ] Optionally publish an alpha candidate to TestPyPI for smoke tests

5. Documentation and Examples
   - [ ] Update `README.md` with full-genome (HG002) usage instructions
   - [ ] Include example command-lines, expected output file names, and sample metrics
   - [ ] Demonstrate `Tools.init(verbose=True)` usage in CLI examples and notebooks
   - [ ] Proofread all updates and ensure consistency with project style

Once all subtasks are complete, tag the release candidate and prepare for testing on broader datasets.
