# .codex/plan_2025-06-04.md
## Plan: 2025-06-04 – α-release Candidate Preparation

### Goal
Prepare the first α-quality release candidate (0.1.0a1) for end-to-end happy benchmarking on HG002.

### Objectives & Subtasks

1. Validate end-to-end benchmarking on full HG002 truth set
   - [ ] Execute existing integration tests against HG002 dataset
   - [ ] Capture performance metrics (runtime, memory) under multi-threaded mode
   - [ ] Identify any data-path issues or failures; triage and fix

2. Implement and verify auto-index creation for all VCF outputs (Task C-5)
   - [ ] Audit `_ensure_vcf_index` usage in hap.py, qfy, and pre commands
   - [ ] Extend `_ensure_vcf_index` to cover JSON, ROC TSV, and any auxiliary files
   - [ ] Write unit tests to confirm index files (.tbi/.csi) are auto-generated

3. Optimize performance for large genomes and multi-threading
   - [ ] Profile HG002 workflow to locate bottlenecks (I/O, parsing, comparison)
   - [ ] Introduce thread pools or process pools where safe in Tools and Haplo.compare
   - [ ] Add a `--threads` flag to CLI wrappers if not already present

4. Finalize packaging and versioning
   - [ ] Bump version to `0.1.0a1` in `setup.cfg` and/or `pyproject.toml`
   - [ ] Update `RELEASES.md` with α1 notes and changelog highlights
   - [ ] Verify `pip install .` installs console scripts (`hap.py`, `qfy`, `pre`)

5. Polish documentation and examples
   - [ ] Update `README.md` and `doc/` with HG002 usage instructions and sample commands
   - [ ] Include examples for `Tools.init(verbose=True)` in tutorials and notebooks
   - [ ] Validate code snippets in documentation with `nox -s docs-build` or equivalent

### Next Steps
- After completing the above, push RC branch `v0.1.0a1-rc1`, tag, and open α1 release PR
- Update roadmap in `.codex/plan_2025-06-05.md` with feedback and remaining β/RC milestones

*Generated 2025-06-04 by codex agent*
