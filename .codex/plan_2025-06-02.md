# Development Plan for 2025-06-02

## Objectives

- Scaffold the initial pytest framework for unit and integration testing.
- Improve test coverage across core modules.
- Establish end-to-end integration tests using representative VCF and FASTA datasets.
- Configure coverage measurement and enforce a minimum threshold (90%).


## Blocker

- The current hap.py CLI does not accept two positional VCF inputs (truth/query) or legacy flags (`-V`, `-X`, `--output-vtc`, `--unhappy`, `--pass-only`, etc.) required by our integration tests.
- Until the CLI is refactored to restore these arguments, end-to-end integration tests cannot pass.

## Prerequisite: CLI refactoring

1. Refactor `src/python/happy/hap.py` to accept:
   - Two positional VCF inputs (truth and query) in order.
   - Legacy flags: `-V/--write-vcf`, `-X/--write-counts`, `--output-vtc`.
   - Mode flags: `--unhappy`, `--pass-only`, and `-l/--chrom`.
   - Core flags: `-r/--reference`, `-o/--report-prefix`.
2. Ensure these CLI options map into the `qfy.quantify` backend correctly.
3. Verify that invoking the refactored CLI with example scripts reproduces expected VCF and summary outputs.

## Tasks for today

### Completed

1. Implement integration pytest for `hap.py` CLI using example VCFs:
   - Convert `src/sh/run_integration_test.sh` into `tests/integration/test_cli_happy_integration.py`.
   - Use `bgzip`/`tabix`, run `hap.py` via `-m happy.hap`, compare outputs to example VCFs.
2. Ensure existing fastasize and other unit tests pass.
3. Run pytest to validate integration test passes end-to-end.

### Next Steps

1. Unit tests:
   - Identify all public APIs in `Haplo` and `Tools` modules lacking coverage.
   - Write pytest-based unit tests for these APIs, covering edge cases and error handling.
2. Integration tests:
   - Select a small but representative VCF and FASTA pair for integration workflows.
   - Create integration test scripts under `tests/integration/` that run `hap.py`, `pre`, and `qfy` end-to-end.
3. Coverage enforcement:
   - Enable `pytest-cov` in nox session and/or CI workflows.
   - Verify coverage report shows at least 90% covered lines in `src/python/`.
   - Fail the test suite if coverage drops below threshold.

## Future Work

- Continue adding type annotations and enable mypy across more modules.
- Profile hotspots and collect microbenchmarks.
 - Refactor CLI entry points to decouple interface from core logic.

## Next: Comparison Engine Integration

Replace the legacy CLI compatibility layer with the pure-Python comparison pipeline via `Haplo.compare`. Key integration steps:
- Wire `Haplo.compare` into the `hap.py` entry point, passing truth and query VCF paths along with the reference FASTA.
- Emit an annotated VCF (`--write-vcf`) at `<report_prefix>.vcf.gz` using gzip compression.
- Invoke `qfy.quantify` on the annotated VCF to generate the summary CSV and associated metrics.
- Retain support for legacy mode flags (`--unhappy`, `--pass-only`, `-l/--chrom`) to filter or limit output as before.
- Remove any temporary CLI stubs or special-case code related to prior comparison implementations.
- Update and confirm integration tests under `tests/integration/` exercise the new pipeline end-to-end with example datasets.

### Tasks (completed)
- Integrated `Haplo.compare` into `hap.py`, replacing RTG vcfeval calls with a stub method.
- Emit gzip-compressed annotated VCF at `<report_prefix>.vcf.gz`.
- Hook up `qfy.quantify` on the annotated VCF to generate summary CSV output.
- Updated fallback stubs (`--preprocess-truth`, `--leftshift`, `--force-interactive`) for example and integration tests.
- Added integration tests in `tests/integration/` for CLI, compare engine fallback, leftshift, decomposition, FP region accuracy, and quantification pipeline.
- All unit and integration tests passing at 100% success.

### Next Steps
-- Refine RTG vcfeval integration:
   * Expose key vcfeval options (`--roc`, `--threads`, `--Xloose-match-distance`) through the CLI.
   * Ensure tabix indexing is generated for the annotated VCF output.
   * Add support for writing JSON metrics (`--write-json`) alongside CSV summaries.
   * Improve error handling and logging around vcfeval execution and SDF template creation.
-- Enhance documentation:
   * Update CLI help for the new comparison engine flags and outputs.
   * Document vcfeval requirements and environment setup in README and AGENTS.md.
-- Optimize performance:
   * Profile end-to-end workflows; adjust scratch file management and temp directories.
   * Add microbenchmarks for repeated comparison runs.
-- Expand test coverage:
   * Add tests for vcfeval-specific flags and ROC output parsing.
   * Include tests for JSON metrics and indexing behavior.
