# Plan for 2025-06-02

## Completed on 2025-06-02
- Committed all changes from the previous session: unit and integration tests, CI workflows, type annotations for VariantProcessor, and help stubs for CLI entrypoints.
- Enabled incremental mypy checks for modernized modules and configured GitHub Actions CI.

## Next Steps
1. Optimize performance:
   - [ ] Develop microbenchmarks (pytest-benchmark) for critical modules:
     - VariantProcessor (`Haplo/variant_processor.py`)
     - FASTA utilities (`Tools/fastasize.py`)
     - VCF extraction (`Tools/vcfextract.py`)
     - ROC computations (`Haplo/happyroc.py`)
   - [ ] Measure baseline timings for key workflows.
2. Profile end-to-end runs:
   - [ ] Instrument `hap.py` CLI with cProfile or pyinstrument to identify hotspots.
   - [ ] Generate profiling reports and flame graphs.
3. Integrate benchmarking into CI:
   - [ ] Extend or add a dedicated `nox -s benchmarks` session to include new module benchmarks.
   - [ ] Set performance regression gates (e.g., 5% slowdown triggers warnings).
4. Document benchmarking strategy:
   - [ ] Update README or doc/ to describe how to run and interpret microbenchmarks and profiles.
   - [ ] Add examples of command-line flags or nox sessions for benchmarking.

## Notes
- Use representative small VCF/FASTA files for microbenchmarks to keep runs fast.
- For profiling complex workflows, capture end-to-end CLI runs on sample data under `/example/integration`.
