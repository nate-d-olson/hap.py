[pytest]
norecursedirs = src/sh build .github
# Custom markers
markers =
    integration: slow end-to-end tests on example datasets
    unit: quick logic tests without external data
    heavy: whole-genome smoke tests (require RUN_HEAVY=1)
# addopts removed; coverage run via CI nox sessions and manual commands
